import React from 'react'

const Button = ({text}) => {
  return (
    
        <button type='button' className=' rounded bg-[#FF4343] px-2 py-1 text-white'>
        {text}
        </button>
    
  )
}

export default Button